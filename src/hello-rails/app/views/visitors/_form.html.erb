<%= form_with(model: visitor, local: true) do |form| %>
  <% if visitor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(visitor.errors.count, "error") %> prohibited this visitor from being saved:</h2>

      <ul>
      <% visitor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ip %>
    <%= form.text_field :ip, id: :visitor_ip %>
  </div>

  <div class="field">
    <%= form.label :visits %>
    <%= form.number_field :visits, id: :visitor_visits %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
